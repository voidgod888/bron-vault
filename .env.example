# =====================================================
# Bron Vault - Environment Variables Template
# =====================================================
# Copy this file to .env.local (or .env for Docker) and fill in your secure passwords
#
# Usage:
#   1. ./setup-vm.sh (Recommended for first setup)
#      OR
#   2. cp .env.example .env
#   3. Edit .env with your configuration
#   4. docker-compose up -d --build
# =====================================================

# -----------------------------------------------------------
# SINGLESTORE DATABASE (MySQL Compatible)
# -----------------------------------------------------------
# The docker image to use.
# Use 'ghcr.io/singlestore-labs/singlestoredb-dev:latest' for license-free dev/testing.
# Use 'singlestore/cluster-in-a-box:latest' for production (Requires License).
SINGLESTORE_IMAGE=singlestore/cluster-in-a-box:latest

# License key (Required for 'singlestore/cluster-in-a-box:latest')
SINGLESTORE_LICENSE_KEY=

# Database Password (used for root access)
MYSQL_PASSWORD=change_me_root_password_123

# Database Name
MYSQL_DATABASE=bronvaultnew

# -----------------------------------------------------------
# REDIS (Job Queue)
# -----------------------------------------------------------
# Connection URL for the application
REDIS_URL=redis://redis:6379

# -----------------------------------------------------------
# APPLICATION SECURITY
# -----------------------------------------------------------
# Secret key for JWT signing (Min 32 chars)
JWT_SECRET=change_me_long_random_string_min_32_chars

# Enable Authentication
NEXT_PUBLIC_ENABLE_AUTH=true

# Environment (development/production)
NODE_ENV=production
